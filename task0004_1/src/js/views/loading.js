define(['backbone', 'routers/default'], function(Backbone, AppRouter) {
    var Loading = Backbone.View.extend({
        initialize: function () {
            
        }, 

        render: function () {
        }
    });
    return Loading;
});